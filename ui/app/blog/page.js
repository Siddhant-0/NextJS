import { Box, Typography } from '@mui/material';
import { green } from '@mui/material/colors';
import React from 'react';

const BlogList = () => {
  return (
    <Box>
      <Typography variant="h1">BlogList</Typography>
    </Box>
  );
};

export default BlogList;
